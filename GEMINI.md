# Antigravity 에이전트 작업 지침 (Rules & Guidelines)

## 1. 기본 행동 및 자율성 원칙
- **자율 판단 우선 (사소한 질문 금지):**
  - 라이브러리 선택, 파일명/변수명 명명, 표준 예외 처리 등은 되묻지 말고 업계 표준과 모범 사례(Best Practice)에 따라 스스로 결정하고 완수할 것.
  - 다음 경우를 제외하고 작업을 중단하고 질문하지 말 것:
    1) 데이터 삭제, 강제 푸시(`git push --force`) 등 비가역적 파괴 작업
    2) 요구사항 상호 모순으로 진행이 불가능한 경우
- **완성형 코드 작성 (코드 생략/목업 절대 금지):**
  - `// TODO:`, `/* 이전 코드 유지 */`, `...기존 코드...` 같은 생략 문법 절대 금지.
  - 가짜 데이터(Mocking)로 얼버무리지 말고 실제 동작 가능한 완전한 코드를 작성할 것.
- **최소 변경 원칙:**
  - 요청받은 작업과 직접 관련 없는 파일이나 기존 정상 로직은 임의로 포맷팅하거나 리팩토링하지 말 것.
- **자가 회복 및 무한 루프 차단:**
  - 동일한 에러가 2회 이상 연속 발생하면 같은 방식을 반복하지 말고 접근 방식을 전환할 것.
  - 원인을 알 수 없을 때는 터미널 stderr 로그 전문을 먼저 분석할 것.

## 2. 개발 환경 및 도구 제약
- **파괴적 명령어 엄격 금지:**
  - `rm -rf`, `git reset --hard`, `git clean -fd`, `DROP TABLE`, `TRUNCATE` 등은 사용자 승인 없이 실행 금지.
- **격리된 환경 준수:**
  - 전역(Global) 패키지를 설치하지 말고 반드시 가상 환경(`venv`, `node_modules`) 내에서만 설치할 것.
  - 패키지 추가 시 `requirements.txt` 또는 `package.json`에 즉시 반영할 것.
- **보안 및 비밀정보 보호:**
  - DB 접속 정보, API 키, 인증 토큰 등은 코드에 하드코딩 금지. 반드시 `.env` 파일과 환경 변수를 사용할 것.

## 3. 코드 품질 및 아키텍처
- **명시적 타입 및 가독성:**
  - 모든 함수/메서드에 명시적인 타입 힌트와 간결한 독스트링(Docstring)을 작성할 것.
- **방어적 프로그래밍:**
  - 외부 입력값(API 파라미터, DB 데이터, 파일 입력)은 반드시 검증 스키마를 거치도록 설계할 것.
  - 실패 시 명확한 에러 메시지와 함께 적절한 상태 코드를 반환할 것.

## 4. 테스트 및 자체 검증
- **자체 검증 의무화:**
  - 백엔드/스크립트: 코드 수정 후 관련 단위 테스트(`pytest` 등)나 테스트 스크립트를 직접 실행해 Pass 여부를 확인할 것.
  - 프론트엔드/UI: 로컬 서버 구동 후 브라우저 에이전트로 화면 이동, 클릭, 폼 전송을 직접 수행해 볼 것.
- **최종 보고 형식:**
  - "이렇게 할까요?" 묻지 말고, **"1) 수정한 파일 목록 2) 테스트/실행 결과 3) 브라우저 캡처/산출물"** 순으로 결과를 요약 보고할 것.
